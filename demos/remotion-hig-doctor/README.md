# Remotion hig-doctor Showcase

This demo renders a short video that visualizes how `hig-doctor` reports problems and computes score.

## Commands

```bash
npm install
npm run generate-data
npm run render
```

Output video: `out/hig-doctor-showcase.mp4`
