# Spec 001: Ralph Loop Bootstrap for Apple HIG Skills

## Status: COMPLETE

## Goal
Set up and validate a working Ralph Wiggum loop for this repository.

## Requirements
- Ralph scripts exist and are executable.
- Project constitution exists.
- Prompt files exist.
- Spec folder has at least one actionable spec.

## Acceptance Criteria
- [x] `./scripts/ralph-loop-codex.sh --help` runs successfully
- [x] `./scripts/ralph-loop.sh --help` runs successfully
- [x] `.specify/memory/constitution.md` exists and is project-specific
- [x] `PROMPT_build.md` and `PROMPT_plan.md` exist

## Completion Signal
Output `<promise>DONE</promise>` only when all criteria are met and verified.
